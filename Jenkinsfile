pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Iam creating pipeline build from SCM'
                sh lscpu
              sh df -h
            }
        }
    }
}
